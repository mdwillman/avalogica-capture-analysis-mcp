# Avalogica Capture Analysis MCP (.env.example)
# Copy to .env for local development:
#   cp .env.example .env

# Server
PORT=8080
LOG_LEVEL=info

# Simple shared-secret auth for the capture HTTP API (optional but recommended for non-local)
# If set, clients should send:
#   X-Capture-Shared-Secret: <value>
CAPTURE_API_SHARED_SECRET=

# Future (not used in stub yet)
# GOOGLE_CLOUD_PROJECT=avalogica-mcp
# GOOGLE_CLOUD_REGION=us-central1
# GCS_CAPTURE_AUDIO_BUCKET=
